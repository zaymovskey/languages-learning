.gamePage {
  height: calc(100vh - var(--navbar-height) - var(--navbar-height));
  overflow: hidden;
  position: relative;
}

.blocked {
  pointer-events: none;
}

.questionWrapper {
  transition: 0.3s;
}

.fade {
  opacity: 0;
}
